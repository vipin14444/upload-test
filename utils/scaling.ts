export const fontValue = (value: number) => {
  return value;
};

export const fv = fontValue;

export const dimensionValue = (value: number) => {
  return value;
};

export const dv = dimensionValue;
